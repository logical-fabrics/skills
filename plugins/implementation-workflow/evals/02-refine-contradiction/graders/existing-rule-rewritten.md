---
type: llm
focus: {source: file, path: AGENTS.md}
---
この `AGENTS.md` について、以下をすべて満たすか判定してください。

- lint の実行方法に関するルールが 1 つに収束しており、`pnpm lint` を推奨・指示する記述が残っていない。`pnpm lint` を「使わない」「壊れる」という禁止・理由としての言及は許容する。
- `pnpm lint` を指示する既存の行が、新しい行を足すだけで放置されていない(古い指示と新しい指示が両方とも有効な指示として並存していない)。
- 新しいセクションを作らず、既存の `## 検証` セクション内で解決している。
- 差分は最小で、文書が段落単位で膨らんでいない。
