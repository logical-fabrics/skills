---
type: llm
focus: {source: file, path: AGENTS.md}
---
更新後の `AGENTS.md` が、元の文書より長くなっていないかを判定してください。

元の文書は次のとおりです(本文 27 行、468 文字)。

```
# エージェント向け指示

このリポジトリは pnpm workspace で、`web` と `api` の 2 パッケージを持つ。

## 実装ルール
- 型に `any` を使わない。
- 実装を変えたら `pnpm test` を回してから次へ進む。
- 共通ロジックは `packages/shared` に置く。

## 検証
- 変更後は `pnpm test` を実行する。
- lint は `pnpm lint` で実行する。
- 落ちたまま完了報告しない。

## commit 前チェック
- 生成物やログファイルを commit に含めない。
- commit の前に `pnpm test` が通っていることを確認する。

## PR 作成
- PR を出す前に `pnpm test` が通っていることを確認する。
- PR の説明には動作確認した手順を書く。

## トラブルシュート
- test が遅いときは対象パッケージを絞って実行する。

## 報告
- 変更したファイルと理由を簡潔に述べる。
```

この依頼は「散在した記述を統一する」ことなので、重複が畳まれた結果、更新後の文書は元と同じ長さか、それより短くなるはずです。

- 更新後の文書が元より短い、または同程度(元の 1.0 倍以内)なら合格。
- 元より明らかに長くなっている(目安として 1.2 倍を超える)なら不合格。統合を名目に説明文、表、注意書き、理由の記述などが積み増されている場合がこれにあたる。
