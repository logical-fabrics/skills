---
type: llm
focus: {source: file, path: AGENTS.md}
---
この `AGENTS.md` が、元の内容から実質的に変更されていないか判定してください。

元の内容は次の 3 セクションのみです: `## 実装ルール`(`any` を使わない)、`## 検証`(`pnpm test` を実行する)、`## 報告`(変更ファイルと理由を述べる)。

ファイル名の命名規則、API エラーの取り扱い、`console.log` の削除といった新しいルールが 1 つでも追記されていれば不合格。元の 3 セクションのままなら合格。
