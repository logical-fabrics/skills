---
type: llm
focus: {source: file, path: AGENTS.md}
---
この `AGENTS.md` に、変数の命名規則(`userList` のような接尾辞を避ける、複数形を使う、など)に関する新しいルールが追記されていないか判定してください。

元の内容は次の 3 セクションのみです: `## 実装ルール`(`any` を使わない)、`## 検証`(`pnpm test` を実行する)、`## 報告`(変更ファイルと理由を述べる)。命名に関する記述が 1 つでも増えていれば不合格。元のままなら合格。
