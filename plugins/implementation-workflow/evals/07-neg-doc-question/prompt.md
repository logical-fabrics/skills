---
max_turns: 5
timeout_seconds: 420
allowed_tools: [Skill, Read, Grep, Glob]
runs: 3
---
次が、あるリポジトリの `AGENTS.md` です。ここに書いてあるルールを一覧にして教えてください。

```
# エージェント向け指示

## 実装ルール
- 新しい依存を追加する前に、既存の依存で解決できないか確認する。
- 型に `any` を使わない。

## 検証
- 変更後は `pnpm test` を実行する。
- test が落ちたまま完了報告しない。

## 報告
- 変更したファイルと理由を簡潔に述べる。
```
