# Source of Truth

判断の優先順位:

1. ユーザーの最新依頼。
2. 現在の repo、docs、schema、tests、UI、config。
3. 現在の公式 docs、Context7、一次情報。
4. ライブシステム。ただし必要な場合のみ。
5. 過去の plan、memory、historical notes。必ず検証対象として扱う。

古い plan、古い docs、既存 skill の文言は source of truth ではない。現在の repo とユーザーの最新意図に照合してから使う。

